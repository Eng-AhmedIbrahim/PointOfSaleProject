@inject NavigationManager _navigationManager
@inject CommonProperties _commonProperties
@inject ISnackbar _snackbar
@inject IDialogService _dialogService
@inject IDeliveryServices _deliveryServices
@inject HandelDeliveryInvocation _handelDeliveryInvocation
@inject LocalizationService Localizer

<MudStack Spacing="0" Class="action-button-container flex-grow-1" Style="height: 100%;">
    @if (_commonProperties.IsFeatureEnabled("Delivery_Button_Order"))
    {
        <AuthorizeView Policy="CanAccessDeliveryOrderBtn">
            <Authorized>
                <MudButton Variant="Variant.Filled"
                           Class="modern-action-button btn-primary-gradient"
                           Size="Size.Large"
                           OnClick='AddCustomer'>
                    <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
                        <MudIcon Icon="@Icons.Material.Filled.CheckCircle" Class="action-icon" />
                        <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["OrderBtn"]</MudText>
                    </MudStack>
                </MudButton>
            </Authorized>
        </AuthorizeView>
    }

    @if (_commonProperties.IsFeatureEnabled("Delivery_Button_AddNew"))
    {
        <AuthorizeView Policy="CanAccessDeliveryAddNewBtn">
            <Authorized>
                <MudButton Variant="Variant.Filled"
                           Class="modern-action-button btn-secondary-gradient"
                           Size="Size.Large"
                           OnClick='AddNewAddress'>
                    <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
                        <MudIcon Icon="@Icons.Material.Filled.LocationCity" Class="action-icon" />
                        <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["AddNew"]</MudText>
                    </MudStack>
                </MudButton>
            </Authorized>
        </AuthorizeView>
    }

    @if (_commonProperties.IsFeatureEnabled("Delivery_Button_Clear"))
    {
        <AuthorizeView Policy="CanAccessDeliveryClearBtn">
            <Authorized>
                <MudButton Variant="Variant.Filled"
                           Class="modern-action-button btn-danger-gradient"
                           Size="Size.Large"
                           OnClick="ClearDeliveryOrder">
                    <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
                        <MudIcon Icon="@Icons.Material.Filled.Clear" Class="action-icon" />
                        <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["Clear"]</MudText>
                    </MudStack>
                </MudButton>
            </Authorized>
        </AuthorizeView>
    }

    @if (_commonProperties.IsFeatureEnabled("Delivery_Button_Complaints"))
    {
        <AuthorizeView Policy="CanAccessDeliveryComplaintsBtn">
            <Authorized>
                <MudButton Variant="Variant.Filled"
                           Class="modern-action-button btn-secondary-gradient"
                           Size="Size.Large"
                           OnClick="ShowComplaints">
                    <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
                        <MudIcon Icon="@Icons.Material.Filled.ReportProblem" Class="action-icon" />
                        <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["Complaints"]</MudText>
                    </MudStack>
                </MudButton>
            </Authorized>
        </AuthorizeView>
    }

    @if (_commonProperties.IsFeatureEnabled("Delivery_Button_Search"))
    {
        <AuthorizeView Policy="CanAccessDeliverySearchBtn">
            <Authorized>
                <MudButton Variant="Variant.Filled"
                           Class="modern-action-button btn-info-gradient"
                           Size="Size.Large">
                    <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
                        <MudIcon Icon="@Icons.Material.Filled.Search" Class="action-icon" />
                        <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["Search"]</MudText>
                    </MudStack>
                </MudButton>
            </Authorized>
        </AuthorizeView>
    }

    @if (_commonProperties.IsFeatureEnabled("Delivery_Button_BranchMgmt"))
    {
        <AuthorizeView Policy="CanAccessDeliveryBranchManagementBtn">
            <Authorized>
                <MudButton Variant="Variant.Filled"
                           Class="modern-action-button btn-info-gradient"
                           Size="Size.Large">
                    <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
                        <MudIcon Icon="@Icons.Material.Filled.ManageAccounts" Class="action-icon" />
                        <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["BranchManagement"]</MudText>
                    </MudStack>
                </MudButton>
            </Authorized>
        </AuthorizeView>
    }

    @if (_commonProperties.IsFeatureEnabled("Delivery_Button_Distribution"))
    {
        <AuthorizeView Policy="CanAccessDeliveryDistributionBtn">
            <Authorized>
                <MudButton Variant="Variant.Filled"
                           Class="modern-action-button btn-warning-gradient"
                           Size="Size.Large">
                    <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
                        <MudIcon Icon="@Icons.Material.Filled.LocalShipping" Class="action-icon" />
                        <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["Distribution"]</MudText>
                    </MudStack>
                </MudButton>
            </Authorized>
        </AuthorizeView>
    }

    @if (_commonProperties.IsFeatureEnabled("Delivery_Button_ToggleDir"))
    {
        <AuthorizeView Policy="CanAccessDeliveryToggleDirectionBtn">
            <Authorized>
                <MudButton Variant="Variant.Filled"
                           Class="modern-action-button btn-success-gradient"
                           Size="Size.Large"
                           OnClick="ToggleDirection">
                    <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
                        <MudIcon Icon="@Icons.Material.Filled.SwapHoriz" Class="action-icon" />
                        <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["ToggleDirection"]</MudText>
                    </MudStack>
                </MudButton>
            </Authorized>
        </AuthorizeView>
    }

    <MudButton Variant="Variant.Filled"
               Class="modern-action-button btn-secondary-gradient"
               Size="Size.Large"
               OnClick="BackToPos">
        <MudStack Row="false" AlignItems="AlignItems.Center" Spacing="1">
            <MudIcon Icon="@Icons.Material.Filled.ArrowBack" Class="action-icon" />
            <MudText Typo="Typo.subtitle2" Style="font-weight: 700;">@Localizer["Back"]</MudText>
        </MudStack>
    </MudButton>
</MudStack>
